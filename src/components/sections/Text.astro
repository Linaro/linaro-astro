---
const { style, text_content } = Astro.props;
import * as marked from "marked";
console.log(text_content)
const markdown = await marked.parseInline(text_content);
---

<p class:list={[style, "prose-lg md:prose-xl px-4"]} set:html={markdown} />
